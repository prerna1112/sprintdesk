import { RouteShell } from './RouteShell';

export default function BoardRoute() {
  return (
    <RouteShell
      eyebrow="Current sprint"
      title="Board"
      description="Kanban interactions will be added without coupling components to raw DTOs."
    />
  );
}
